# =============================================================================
# NageshCare Testing Environment - MINIMAL CONFIG
# =============================================================================
# Purpose: Test deployment with minimum variables (matches sumithrakp pattern)
# Use this to isolate the 503 error cause
# If this works, add variables back one by one
# =============================================================================

# Core Django Settings
SECRET_KEY=_qs_xg&$bt742ag7z%$j3bloue)xzxnt^^8fn-nod7mu@oa^q-
DEBUG=False
ALLOWED_HOSTS=nageshcare.com,www.nageshcare.com

# Database Configuration (using separate variables - your settings support this)
DB_NAME=wrgccpiz_nageshcare_db
DB_USER=wrgccpiz_nageshcare_user
DB_PASSWORD=fxLF_4MxCCgvFP6

# SSL Settings - DISABLED (like working sumithrakp site)
# Enable these AFTER confirming the site works
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# =============================================================================
# TESTING NOTES:
# =============================================================================
# 1. Copy this to server: /home/wrgccpiz/public_html/nageshcare/.env
# 2. Replace existing .env with this minimal version
# 3. Restart application: touch passenger_wsgi.py
# 4. Test: curl -I https://nageshcare.com
# 5. If it works (HTTP 200), SSL was the problem
# 6. Then gradually add back: CSRF_TRUSTED_ORIGINS, SSL settings, etc.
# =============================================================================

# Minimal .htaccess for cPanel Python App compatibility
# This file is required by cPanel but should be minimal
# Main Passenger configuration is in /home/wrgccpiz/public_html/.htaccess

# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "\.(env|ini|log|conf|sql|sqlite3|bak|backup)$">
    Require all denied
</FilesMatch>
